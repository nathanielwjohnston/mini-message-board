<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Index</title>
  </head>
  <body>
    <%- include("navbar") %> <% for (let message of messages) { %>
    <h3><%= message.user %></h3>
    <p><%= message.text %></p>
    <p><%= message.added %></p>
    <a href="/message/<%= message.id %>">View Message</a>
    <% } %>
  </body>
</html>
